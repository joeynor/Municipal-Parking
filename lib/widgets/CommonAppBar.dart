import 'package:flutter/material.dart';

class CommonAppBar extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text("Municipal Parking"),
    );
  }
}